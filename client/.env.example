# In dev, requests to /api are proxied to this target (see vite.config.js).
VITE_API_TARGET=http://localhost:8791
# For a production build served separately from the API, set the absolute base:
# VITE_API_BASE=https://rocky.yourdomain.com
